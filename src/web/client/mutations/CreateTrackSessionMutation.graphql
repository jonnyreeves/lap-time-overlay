
mutation CreateTrackSessionMutation($date: String!, $format: String!, $circuitId: ID!) {
  createTrackSession(date: $date, format: $format, circuitId: $circuitId) {
    id
    date
    format
    circuit {
      id
      name
    }
  }
}
