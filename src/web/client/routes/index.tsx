import { Card } from "../components/index.js";

export function HomePage() {
  return (
    <Card title="Get started">
      <p className="value">
        Uploads and overlays will live here once auth is wired into the rest of the flow.
      </p>
    </Card>
  );
}
